using System.Collections;
using System.Collections.Generic;
using TMPro.Examples;
using UnityEngine;
using TMPro;
using UnityEngine.UI;

public class DashIconBehaviour : MonoBehaviour
{
   // TextMeshProUGUI label;
//   Image overlay;
 //   float cooldown;
//float cooldownRate;

    // Start is called before the first frame update
    void Start()
    {
       // label = GetComponentInChildren<TextMeshProUGUI>();
       // Images[] images = GetComponentsInChildren<Image>();
       // for (int i = 0; i < images.Length; i++)
      //  {
         //   if (images[i].tage == "overlay")
        //    {
       //         overlay = images[i];
       //     }
       // }
     // cooldownRate = PinBehaviour.cooldownRate;
     //   overlay.fillAmount = 0.0f;
        
    }

    // Update is called once per frame
    void Update()
    {
       // cooldown = PinBehaviour.cooldown;
       // string message = "";
       // if (cooldown > 0.0)
       // {
       //     float fill = cooldown / cooldownRate;
       //     message = string.Format("{0:0.0}", cooldown);
            // message = cooldown.ToString();
        //    overlay.fillAmount = fill;
       // }
       // label.SetText(message);
        
    }
}
