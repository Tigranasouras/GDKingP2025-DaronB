using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Random : MonoBehaviour
{
   
    // Update is called once per frame
    void Update()
    {
        float randomX = Random.Range(M)
    }


  
}
